const red = {
  default: 'red'
}

const blue = {
  default: 'blue'
}

const green = {
  default: 'green'
}

export default {
  black: 'black',
  white: 'white',
  primary: green.default,
  secondary: blue.default,
  error: red.default,
}
