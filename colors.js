const red = {
  default: 'red'
}

const blue = {
  default: 'blue'
}

const green = {
  default: 'green'
}

export default {
  primary: green.default,
  secondary: blue.default,
  error: red.default
}
